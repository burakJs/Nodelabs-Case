import 'package:flutter/material.dart';

final class AppPadding extends EdgeInsets {
  /// All 4 padding
  const AppPadding.allLow() : super.all(4);

  /// All 8 padding
  const AppPadding.allMedium() : super.all(8);

  /// All 12 padding
  const AppPadding.allHigh() : super.all(12);

  /// All 16 padding
  const AppPadding.allXHigh() : super.all(16);

  /// All 24 padding
  const AppPadding.allXXHigh() : super.all(24);

  /// All 32 padding
  const AppPadding.allXXXHigh() : super.all(32);

  /// Vertical 4 padding
  const AppPadding.verticalLow() : super.symmetric(vertical: 4);

  /// Vertical 8 padding
  const AppPadding.verticalMedium() : super.symmetric(vertical: 8);

  /// Vertical 12 padding
  const AppPadding.verticalHigh() : super.symmetric(vertical: 12);

  /// Vertical 16 padding
  const AppPadding.verticalXHigh() : super.symmetric(vertical: 16);

  /// Vertical 24 padding
  const AppPadding.verticalXXHigh() : super.symmetric(vertical: 24);

  /// Vertical 32 padding
  const AppPadding.verticalXXXHigh() : super.symmetric(vertical: 32);

  /// Horizontal 4 padding
  const AppPadding.horizontalLow() : super.symmetric(horizontal: 4);

  /// Horizontal 8 padding
  const AppPadding.horizontalMedium() : super.symmetric(horizontal: 8);

  /// Horizontal 12 padding
  const AppPadding.horizontalHigh() : super.symmetric(horizontal: 12);

  /// Horizontal 16 padding
  const AppPadding.horizontalXHigh() : super.symmetric(horizontal: 16);

  /// Horizontal 24 padding
  const AppPadding.horizontalXXHigh() : super.symmetric(horizontal: 24);

  /// Horizontal 32 padding
  const AppPadding.horizontalXXXHigh() : super.symmetric(horizontal: 32);

  /// Only top 4 padding
  const AppPadding.topLow() : super.only(top: 4);

  /// Only top 8 padding
  const AppPadding.topMedium() : super.only(top: 8);

  /// Only top 12 padding
  const AppPadding.topHigh() : super.only(top: 12);

  /// Only top 16 padding
  const AppPadding.topXHigh() : super.only(top: 16);

  /// Only top 24 padding
  const AppPadding.topXXHigh() : super.only(top: 24);

  /// Only top 32 padding
  const AppPadding.topXXXHigh() : super.only(top: 32);

  /// Only right 4 padding
  const AppPadding.rightLow() : super.only(right: 4);

  /// Only right 8 padding
  const AppPadding.rightMedium() : super.only(right: 8);

  /// Only right 12 padding
  const AppPadding.rightHigh() : super.only(right: 12);

  /// Only right 16 padding
  const AppPadding.rightXHigh() : super.only(right: 16);

  /// Only right 24 padding
  const AppPadding.rightXXHigh() : super.only(right: 24);

  /// Only right 32 padding
  const AppPadding.rightXXXHigh() : super.only(right: 32);

  /// Only bottom 4 padding
  const AppPadding.bottomLow() : super.only(bottom: 4);

  /// Only bottom 8 padding
  const AppPadding.bottomMedium() : super.only(bottom: 8);

  /// Only bottom 12 padding
  const AppPadding.bottomHigh() : super.only(bottom: 12);

  /// Only bottom 16 padding
  const AppPadding.bottomXHigh() : super.only(bottom: 16);

  /// Only bottom 24 padding
  const AppPadding.bottomXXHigh() : super.only(bottom: 24);

  /// Only bottom 32 padding
  const AppPadding.bottomXXXHigh() : super.only(bottom: 32);

  /// Only left 4 padding
  const AppPadding.leftLow() : super.only(left: 4);

  /// Only left 8 padding
  const AppPadding.leftMedium() : super.only(left: 8);

  /// Only left 12 padding
  const AppPadding.leftHigh() : super.only(left: 12);

  /// Only left 16 padding
  const AppPadding.leftXHigh() : super.only(left: 16);

  /// Only left 24 padding
  const AppPadding.leftXXHigh() : super.only(left: 24);

  /// Only left 32 padding
  const AppPadding.leftXXXHigh() : super.only(left: 32);
}
